import React, { Component } from "react";
import styles from "./Menu.module.css";
export default class MenuButton extends Component {
  render() {
    return <button className={styles.menuButton}></button>;
  }
}
